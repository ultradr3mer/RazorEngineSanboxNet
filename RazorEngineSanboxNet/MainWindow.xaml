<!-- MainWindow.xaml — RazorEngineSandbox (Dark Mode) -->
<Window x:Class="RazorEngineSandbox.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="RazorEngineSandbox"
        Height="900" Width="1400"
        WindowStartupLocation="CenterScreen"
        Background="#1e1e1e" Foreground="#dddddd">
    <Grid Background="#1e1e1e">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <DockPanel Grid.Row="0" LastChildFill="True" Margin="6,6,6,0">
            <Button x:Name="RenderBtn" Content="Render" Padding="16,6" Margin="0,0,8,0" DockPanel.Dock="Left" Background="#2d2d30" Foreground="#dddddd"/>
            <CheckBox x:Name="AutoRenderChk" Content="Auto-Render" VerticalAlignment="Center" Margin="0,0,12,0" DockPanel.Dock="Left" Foreground="#dddddd"/>
            <TextBlock x:Name="StatusText" Text="Bereit" VerticalAlignment="Center" Foreground="#aaaaaa"/>
        </DockPanel>

        <!-- Dreigeteilte Ansicht -->
        <Grid Grid.Row="1" Margin="6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Links: C#-Scripting (Model) -->
            <TextBox x:Name="ScriptBox"
                     Grid.Column="0"
                     AcceptsReturn="True" AcceptsTab="True"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     FontFamily="Consolas" FontSize="13"
                     TextWrapping="NoWrap" Padding="8"
                     Background="#252526" Foreground="#dcdcdc"/>

            <GridSplitter Grid.Column="1" Width="6" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="#333333"/>

            <!-- Mitte: Razor-Template -->
            <TextBox x:Name="RazorBox"
                     Grid.Column="2"
                     AcceptsReturn="True" AcceptsTab="True"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     FontFamily="Consolas" FontSize="13"
                     TextWrapping="NoWrap" Padding="8"
                     Background="#252526" Foreground="#dcdcdc"/>

            <GridSplitter Grid.Column="3" Width="6" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="#333333"/>

            <!-- Rechts: HTML-Preview -->
            <WebBrowser x:Name="HtmlPreview" Grid.Column="4"/>
        </Grid>

        <!-- Fehlerleiste -->
        <TextBlock x:Name="ErrorText" Grid.Row="2" Margin="6,0,6,6" Foreground="#ff6a6a" TextWrapping="Wrap"/>
    </Grid>
</Window>